# ── Runtime dependencies ───────────────────────────────────────────────────────
requests>=2.31.0
urllib3>=2.0.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
openpyxl>=3.1.0
python-docx>=1.1.0

# ── LinkedIn crawler (new — isolated from HBI UCalgary pipeline) ───────────────
playwright>=1.44.0       # headless browser automation for LinkedIn
python-dotenv>=1.0.0     # load .env credentials file

# ── Build-time (for creating the .exe) ────────────────────────────────────────
# pyinstaller>=6.0.0   # uncomment before running build.bat
